<Window x:Class="WpfApp4.MainWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp4"
       
    Title="Window1" Height="400" Width="500" Loaded="Window_Loaded" WindowState="Maximized" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <TreeView x:Name="foldersItem" PreviewMouseRightButtonDown="foldersItem_PreviewMouseRightButtonDown" SelectedItemChanged="foldersItem_SelectedItemChanged" Width="Auto" Background="#FFFFFFFF" BorderBrush="#FFFFFFFF" Foreground="#FFFFFFFF" Grid.ColumnSpan="2" Margin="0,27,295.6,0.4">
           
            <TreeView.Resources>
               
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Name="img"  Width="20" Height="20" Stretch="Fill" 
                                       Source="{Binding
                                       RelativeSource={RelativeSource 
                                       Mode=FindAncestor, 
                                       AncestorType={x:Type TreeViewItem}}, 
                                       Path=Tag, 
                                       Converter={x:Static local:HeaderToImageConverter.Instance}}"       
                                       />
                                    <TextBlock Text="{Binding}" Margin="5,0" />
                                    
                                </StackPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.Resources>
        </TreeView>


        <TabControl   HorizontalAlignment="Left" Height="1000" Margin="0.4,0,0,0" VerticalAlignment="Top" Width="1000" Grid.Column="1">
            <TabItem Header="Files Control" Margin="-2,-2,-2.4,0">
                <Grid Background="White">
                <StackPanel>
                        <ListView Name="Thumbnails" MouseDoubleClick="ThumbnailsOpenFile" Grid.Column="1" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.CanContentScroll="False">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Image Source="{Binding}" Height="30" Width="30" Margin="5"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Tag Control">
                <Grid Background="White">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Height="30">
                
                        <Label Width="60" Height="100" HorizontalAlignment="Left" >File Tags:</Label>
                        <TextBox Name="TagsOutput" Height="100" Width="200" HorizontalAlignment="Left"/>
                        <Button Margin="10,0,0,0" Click="saveTags">Save Tags</Button>
                       
                    </StackPanel>
                   
                </Grid>
            </TabItem>
            <TabItem Header="Views">
                <Grid Background="White">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                        <Label Width="74" Height="34" HorizontalAlignment="Left" VerticalAlignment="Top" >tag for view</Label>
                        <TextBox Name="viewTxt" Height="34" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <Button Height="25" HorizontalAlignment="Left" VerticalAlignment="Top" Click="getView">Create view</Button>
                        
                    </StackPanel>
                    
                    <TreeView x:Name="viewTree" Width="Auto" Background="#FFFFFFFF" BorderBrush="#FFFFFFFF" Foreground="#FFFFFFFF" Grid.ColumnSpan="2" Margin="0,27,295.6,0.4">

                        <TreeView.Resources>

                            <Style TargetType="{x:Type TreeViewItem}">
                                <Setter Property="HeaderTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Image Name="img"  Width="20" Height="20" Stretch="Fill" 
                                       Source="{Binding
                                       RelativeSource={RelativeSource 
                                       Mode=FindAncestor, 
                                       AncestorType={x:Type TreeViewItem}}, 
                                       Path=Tag, 
                                       Converter={x:Static local:HeaderToImageConverter.Instance}}"       
                                       />
                                                <TextBlock Text="{Binding}" Margin="5,0" />

                                            </StackPanel>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TreeView.Resources>
                    </TreeView>

                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>